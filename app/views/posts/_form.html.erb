<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" class="post-input">
    <%= f.text_area :text %>
  </div>

  <%= f.hidden_field :user_id, value: @post.user_id %>
  <%= f.hidden_field :upvotes, value: @post.upvotes %>
  <%= f.hidden_field :downvotes, value: @post.downvotes %>

  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
